# ===========================================
# LOCAL DOCKER TESTING - Ready to use!
# ===========================================
# Just copy this file: copy .env.docker .env
# Then run: docker-compose up --build

# Node Environment
NODE_ENV=development

# MongoDB (Docker will create this automatically)
MONGO_USERNAME=admin
MONGO_PASSWORD=localpass123

# JWT Secret (use any random string for local testing)
JWT_SECRET=local-dev-jwt-secret-key-12345

# Client URL
CLIENT_URL=http://localhost

# ===========================================
# OPTIONAL - Only fill if you need these features
# ===========================================

# Cloudinary (for image uploads)
# Leave empty if not using image uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Google OAuth (for Google login)
# Leave empty if not using Google login
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
